# name: GitHub Classroom Autograding

# on:
#   push:
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   autograding:
#     runs-on: ubuntu-latest
#     timeout-minutes: 15

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Show autograding.json (debug)
#         run: |
#           echo "Commit: $GITHUB_SHA"
#           echo "----- BEGIN .github/classroom/autograding.json -----"
#           if [ -f .github/classroom/autograding.json ]; then
#             cat .github/classroom/autograding.json
#           else
#             echo "autograding.json NOT FOUND at .github/classroom/autograding.json"
#             echo "Directory listing:"
#             ls -la .github/classroom || true
#           fi
#           echo "----- END autograding.json -----"

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'

#       - name: Run Autograding
#         uses: education/autograding@v1
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
